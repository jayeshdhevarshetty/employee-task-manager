services:
  redis:
    image: redis:latest
    networks:
      - jd_network
    


  fastapi:
    container_name: employee-task-manager
    build: .
    ports:
      -"8005:8005"
    networks:
      - jd_network
    depends_on:
      - redis:

networks:
  jd_network: